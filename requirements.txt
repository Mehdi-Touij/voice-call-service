# Core Pipecat with required extras
pipecat-ai[daily,deepgram,elevenlabs,silero,openai]==0.0.40

# Explicit core dependencies to avoid conflicts
aiohttp==3.9.1
aiohttp-cors==0.7.0
httpx==0.25.2

# Audio processing
numpy==1.24.4
soundfile==0.12.1
webrtcvad==2.0.10

# Torch for Silero VAD (specific versions for compatibility)
torch==2.1.2
torchaudio==2.1.2

# Required by Daily.co
daily-python==0.9.0

# Async support
asyncio-throttle==1.0.2

# JSON handling
orjson==3.9.10

# System dependencies
setuptools==69.0.2
wheel==0.42.0

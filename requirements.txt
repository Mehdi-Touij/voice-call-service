# Bulletproof Pipecat requirements - tested for Railway deployment
pipecat-ai==0.0.40

# Install Pipecat extras separately to avoid conflicts
daily-python>=0.9.0
deepgram-sdk>=3.2.6
elevenlabs>=0.2.26

# VAD dependencies - critical for voice detection
silero-vad>=4.0.0
torch>=2.0.0,<3.0.0
torchaudio>=2.0.0,<3.0.0

# Core dependencies
numpy>=1.24.0,<2.0.0
soundfile>=0.12.1

# Web framework
aiohttp>=3.8.5
aiohttp-cors>=0.7.0
httpx>=0.24.1

# Audio processing
webrtcvad>=2.0.10

# System dependencies for Railway
setuptools>=68.0.0
wheel>=0.40.0

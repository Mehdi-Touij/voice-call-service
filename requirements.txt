# Core dependencies with pinned versions
pipecat[daily,deepgram,elevenlabs]==0.0.40
fastapi==0.104.1
uvicorn==0.24.0
httpx==0.25.2
python-dotenv==1.0.0

# Additional dependencies for production
python-multipart==0.0.6  # For file uploads
aiofiles==23.2.1  # For async file operations

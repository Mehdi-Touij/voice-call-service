# Core Pipecat with required extras
pipecat-ai[daily,deepgram,elevenlabs,silero,openai]==0.0.40

# Let pipecat-ai handle aiohttp version (requires >=3.10.3)
# Only specify other explicit dependencies
aiohttp-cors==0.7.0
httpx==0.25.2

# Audio processing
numpy==1.24.4
soundfile==0.12.1
webrtcvad==2.0.10

# Torch for Silero VAD (specific versions for compatibility)
torch==2.1.2
torchaudio==2.1.2

# System dependencies
setuptools==69.0.2
wheel==0.42.0

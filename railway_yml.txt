# railway.yml - Force Railway to use correct start command
services:
  voice-service:
    source: .
    start: "python main.py"
    build:
      nixPacks: true
    variables:
      PYTHONUNBUFFERED: "1"
      PYTHONDONTWRITEBYTECODE: "1"
    healthcheck:
      command: "curl -f http://localhost:8080/health || exit 1"
      interval: 30s
      timeout: 10s
      retries: 3
